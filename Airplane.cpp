#include <bits/stdc++.h>
#define FastIO ios_base::sync_with_stdio(false)

using namespace std;

int main()
{
    FastIO;
    int n;
    while(true) {
        cin >> n;
        if(n == 0) return 0;
        cout << "1" << "/" << "2" << "\n";
    }

    return 0;
}